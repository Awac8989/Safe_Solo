import 'package:flutter/material.dart';

class CommunityRadarPage extends StatelessWidget {
  const CommunityRadarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cộng đồng'),
      ),
      body: const Center(
        child: Text('Community Radar Page - Coming Soon'),
      ),
    );
  }
}