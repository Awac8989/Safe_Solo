import 'package:flutter/material.dart';

class StealthPage extends StatelessWidget {
  const StealthPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ẩn danh'),
      ),
      body: const Center(
        child: Text('Stealth Page - Coming Soon'),
      ),
    );
  }
}